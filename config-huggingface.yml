# NightVisionPro — готовый конфиг под Hugging Face Space (публичный)
# Положи в plugins/NightVision/config.yml и перезапусти сервер или /nvp reload

debug: false
outputDirectory: "data"

detection:
  enabled: true
  server-type: "huggingface"
  endpoint: "https://JavaLoger-nvptop.hf.space"
  api-key: ""
  sample-size: 40
  sample-interval: 10
  worldguard:
    enabled: false
    disabled-regions:
      - "spawn:spawn"

alerts:
  threshold: 0.05
  console: true

violation:
  threshold: 40
  reset-value: 20
  multiplier: 100.0
  decay: 0.35
  vl-decay:
    enabled: true
    interval: 200
    amount: 1

penalties:
  min-probability: 0.50
  custom-alert-prefix: "&6[NVP] &f"
  animation:
    enabled: true
    duration: 80
  actions:
    1: "{CUSTOM_ALERT} &c{PLAYER} &7is suspicious &8(&fVL:&e{VL}&8, &fProb:&e{PROBABILITY}&8)"
    2: "{KICK} kick {PLAYER} NVP Detection (VL:{VL})"
    3: "{BAN} ban {PLAYER} Cheating (NVP VL:{VL})"

messages:
  prefix: "&6[NVP] &r"
  alerts-enabled: "&aAlerts enabled"
  alerts-disabled: "&eAlerts disabled"
  alert-format: "&c{PLAYER} &7| &6Prob: &f{PROBABILITY} &7| &6Buffer: &f{BUFFER}"
  alert-format-vl: "&c{PLAYER} &7| &6Prob: &f{PROBABILITY} &7| &6Buffer: &f{BUFFER} &7| &cVL: {VL}"
  unknown-command: "&cUnknown command: &f{ARGS}"
  players-only: "&cThis command is for players only"
  no-permission: "&cYou don't have permission"
  player-not-found: "&cPlayer not found: &f{PLAYER}"
  config-reloaded: "&aConfiguration reloaded!"
  tracking-stopped: "&eTracking stopped"
  tracking-started: "&aTracking player &f{PLAYER}&a. Use &f/nvp prob &aagain to stop"
  prob-usage: "&eUsage: &f/nvp prob <player>"
  player-offline: "&cPlayer is offline"
  actionbar-format: "&c{PLAYER} &7| &6Prob: &f{PROBABILITY} &7| &eBuffer: &f{BUFFER} &7| &2VL: &f{VL}"
  data-status-header: "&e=== Data Collection Status ==="
  active-sessions: "&fActive sessions: &e{COUNT}"
  no-active-sessions: "&7No active data collection sessions"
  start-hint: "&7Use &e/nvp start <NICK|GLOBAL> <CHEAT|LEGIT|UNLABELED> &7to start"
  session-started: "&aStarted &f{LABEL} &asession for &f{PLAYER} &7(&f{COUNT} &aplayer(s)&7)"
  session-stopped: "&eStopped session for &f{PLAYER}"
  all-sessions-stopped: "&eStopped &f{COUNT} &esessions"
  no-sessions-to-stop: "&7No active sessions to stop"
  no-players-online: "&cNo players online"
  invalid-label: "&cInvalid label: &f{LABEL}"
  valid-labels: "&7Available labels: &aCHEAT, LEGIT, UNLABELED"
  usage-header: "&eUsage:"
  usage-start: "&7  /nvp start <NICK|GLOBAL> <CHEAT|LEGIT|UNLABELED> \"<comment>\""
  usage-stop: "&7  /nvp stop <NICK|GLOBAL>"
  usage-alerts: "&7  /nvp alerts - Toggle AI alerts"
  usage-prob: "&7  /nvp prob <player> - Track probability (actionbar)"
  usage-datastatus: "&7  /nvp datastatus - Data collection status"
  usage-reload: "&7  /nvp reload - Reload configuration"

folia:
  enabled: true
  thread-pool-size: 0
  entity-scheduler:
    enabled: true
  region-scheduler:
    enabled: true
